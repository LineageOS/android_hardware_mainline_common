# Mock Backend

Mock sensor backend for the Mainline Sensors HAL.

## Overview

Provides fake sensor data for all supported sensor types. Used for testing and as a fallback when no real hardware sensors are detected.

This is the least preferred backend and is loaded last by default.
